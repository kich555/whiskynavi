name: Graphify Monthly

# 매월 1일 00:00 UTC (한국시간 09:00)에 실행.
# code-only 갱신은 LLM 불필요. 리포트를 아티팩트로 업로드하고 이슈로 요약을 남긴다.
# 수동 실행도 가능 (workflow_dispatch).
on:
  schedule:
    - cron: "0 0 1 * *"
  workflow_dispatch:

permissions:
  contents: read
  issues: write

concurrency:
  group: graphify-monthly
  cancel-in-progress: false

jobs:
  graphify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          version: "latest"

      - name: Install graphify
        run: uv tool install graphifyy

      - name: Add graphify to PATH
        run: echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Build graph (code-only, no LLM)
        # LLM이 없으므로 code 파일만 AST 추출. docs/papers/images는 스킵.
        # 전체 재구축으로 매월 일관된 베이스라인 유지 (--force).
        run: graphify update . --force

      - name: Cluster + regenerate report (CI mode)
        # --no-viz: 큰 그래프/CI에서 HTML 생성 스킵
        # --no-label: LLM 커뮤니티 명명 스킵 (빈 토큰)
        run: graphify cluster-only . --no-viz --no-label

      - name: Verify outputs
        id: verify
        run: |
          if [ ! -f graphify-out/GRAPH_REPORT.md ]; then
            echo "GRAPH_REPORT.md not found" >&2
            exit 1
          fi
          # 헤더(날짜/요약) 추출 — 이슈 본문용
          {
            echo "summary<<EOF"
            head -12 graphify-out/GRAPH_REPORT.md
            echo "EOF"
          } >> "$GITHUB_OUTPUT"
          echo "report_size=$(wc -c < graphify-out/GRAPH_REPORT.md)" >> "$GITHUB_OUTPUT"

      - name: Upload report artifact
        uses: actions/upload-artifact@v4
        with:
          name: graph-report-${{ github.run_id }}
          path: |
            graphify-out/GRAPH_REPORT.md
            graphify-out/graph.json
            graphify-out/manifest.json
          retention-days: 90

      - name: Create monthly issue
        uses: actions/github-script@v7
        with:
          script: |
            const runId = context.runId;
            const repo = context.repo.owner + "/" + context.repo.repo;
            const date = new Date().toISOString().slice(0, 10);
            const artifactUrl = `https://github.com/${repo}/actions/runs/${runId}`;

            const summary = process.env.SUMMARY || "(summary extraction failed)";
            const reportSize = process.env.REPORT_SIZE || "?";

            const body = [
              `## graphify 월간 리포트 (${date})`,
              ``,
              `> 워크플로: \`.github/workflows/graphify-monthly.yml\``,
              `> 실행: ${artifactUrl}`,
              `> 리포트 크기: ${reportSize} bytes`,
              ``,
              `### 헤더 요약`,
              "```",
              summary,
              "```",
              ``,
              `### 리포트`,
              `전체 리포트는 워크플로 아티팩트 \`graph-report-${runId}\`에서 다운로드 (90일 보관).`,
              ``,
              `### 리뷰 체크리스트 (docs/agents/graphify-workflow.md)`,
              `- [ ] God nodes 상위 10 확인 (의도적 중앙 추상화 vs 커플링)`,
              `- [ ] 커뮤니티 수 / 평균 크기 급격 변화`,
              `- [ ] 단절된 노드 / weakly-connected`,
              `- [ ] 순환 의존성`,
              `- [ ] docs/superpowers/mappings.md 갱신 필요 여부`,
              `- [ ] 발견된 이슈 → 별도 이슈화`,
              ``,
              `자동 생성됨. 라벨은 수동 부여 (needs-triage 등).`,
            ].join("\n");

            const issue = await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `graphify 월간 리포트 — ${date}`,
              body,
            });
            core.info(`Created issue #${issue.data.number}`);
        env:
          SUMMARY: ${{ steps.verify.outputs.summary }}
          REPORT_SIZE: ${{ steps.verify.outputs.report_size }}
